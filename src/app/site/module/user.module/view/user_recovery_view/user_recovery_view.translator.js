export const translator = (lang) => {
    let language;

    switch (true) {
        case (lang === 'es'):
            language = {

            };
            break;

        default:
            language = {

            };
    }

    return language;
}